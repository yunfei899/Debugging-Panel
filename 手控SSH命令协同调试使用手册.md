# 手控 SSH 命令协同调试使用手册

本手册是跨工程使用说明，不绑定某个工程目录、源文件、行号或历史 PID。源码断点、源码单步、变量读取、日志读取和运行时权限切换已在 ARM 手控上验证；通用命令工具尚未封装。以下命令为流程模板，尖括号内容必须先替换为现场核对结果。

## 1. 平时直接怎么说

工程的 AGENTS.md 配置本手册入口后，直接提出任务即可，不需要每次重复工程文件夹和连接信息。

| 目的 | 可以直接发给 AI |
| --- | --- |
| 连接检查 | 连接当前上位机工程对应的手控，先检查程序和日志，不暂停。 |
| 查看最近操作 | 看看我刚才做了什么，对比最新操作和报警记录。 |
| 设置断点 | 在我指定的代码位置打断点，命中后读取我关注的变量。 |
| 单步验证 | 在当前断点处单步一次，比较前后变量和执行位置。 |
| 自动调通 | 自主调通当前工程的源码调试，允许必要的构建、部署和暂停，完成后恢复原程序。 |
| 保持暂停 | 命中后保持暂停，等我下一步指令。 |
| 结束 | 结束本次调试，清理本次新增断点，解除附加并恢复原运行状态。 |

断点使用用户当次指定的文件、函数或行号，不复用上一次验证位置。IDE 当前选区和工作区可以辅助识别目标；多个工程仍无法区分时，只询问缺少的关键信息。

“看日志”不包含编译、暂停、写变量或切换权限；明确要求自主调通时，按已有授权完成必要步骤，不重复询问。

## 2. 为什么 Qt Creator 没有动

```text
Windows：与 AI 对话
  ├─ 网络映射 → Ubuntu 上的当前源码
  └─ SSH → Ubuntu
              ├─ 当前工程的 ARM 编译链 → 构建程序
              ├─ SSH/SFTP → 手控：读日志、传文件、启动/停止
              └─ ARM GDB → 手控 gdbserver → 上位机进程
                              断点、暂停、单步、读变量
```

网络映射负责文件访问，SSH 负责远程执行命令，GDB/gdbserver 负责调试。无需点击 Qt Creator，独立 GDB 的断点和执行位置也不会同步到 Qt Creator。实体手控程序仍会在断点处暂停。

IAR 工具通过窗口消息及 C-SPY 宏复用已有 IAR 会话；这里使用独立 GDB 会话。不要让 Qt Creator 和独立 GDB 同时调试同一个目标进程。曾经验证过的 Qt Creator 界面、剪贴板操作不是本手册的日常方案。

## 3. 每次怎样找到当前工程和设备

AI 接入时执行以下核对，不要求用户反复手填工程路径：

1. 根据当前任务、IDE 文件和工作区识别目标上位机；分别确认它实际使用 Git 还是 SVN。当前终端可能在下位机工程，不能据此认定目标也是下位机。
2. 查看 Windows 网络映射，找到该工程对应的 Ubuntu 目录。映射可有多层，核对 .pro 和目标源码内容，不能只靠目录同名判断。
3. 读取该工程已有构建配置、.pro.user、对应 Qt Creator Kit 和编译链启动脚本，确认编译器、Qt、GDB、输出目录及运行参数。不为读配置启动 Qt Creator。
4. 结合工程绑定的设备配置和当前连接确认手控地址；不能只因为某个 IP 可达就认为是目标手控。
5. 在手控用 ps 和 /proc 核对进程、exe、cwd、参数、TracerPid；已有调试连接先查明归属，不抢占连接。
6. 选择与目标程序对应的本地 ELF、调试符号和源码，必要时比较校验值及构建身份。记录原运行方式供恢复。

以下是已验证环境的连接线索，可复用但不是所有工程的默认值：

| 项目 | 已验证环境 |
| --- | --- |
| Ubuntu | harry@ubuntu，曾解析为 192.168.102.132，Ubuntu 18.04 |
| Windows 密钥 | %USERPROFILE%\.ssh\id_rsa_ubuntu |
| 网络共享根 | \\ubuntu\mold_injection |
| 编译链入口线索 | Ubuntu 用户目录下 qt-hand.sh |
| ARM Kit 名称线索 | ARM_QT_HAND_07 |
| 手控 | root@192.168.4.3，ARMv7 |
| 手控程序目录线索 | /opt/Qt/apps |
| 运行参数线索 | Multi-axisManipulatorSystem -qws |

工程目录、PID、日志路径、调试端口由本次实际环境决定。文档不保存密码和私钥内容。

Windows 登录 Ubuntu 的例子：

```powershell
ssh -i "$env:USERPROFILE/.ssh/id_rsa_ubuntu" -o IdentitiesOnly=yes -o BatchMode=yes harry@ubuntu
```

Ubuntu 登录手控：

```sh
ssh <手控用户>@<已确认手控地址>
```

如果 SSH 主机密钥变化，先核对原因。登录失败如实报告，不把网络可达当成登录成功。

## 4. 新建源码副本，还是使用原工程

**长期使用推荐：一份源码，独立的 ARM Debug 构建目录。临时验证才复制源码。**

| 方式 | 好处 | 代价和适用边界 |
| --- | --- | --- |
| 原源码 + 独立 Debug 构建目录 | 改代码后直接调试，不会调到旧副本；Debug/Release 目标文件分开 | 需确认工程支持异地构建，并隔离打包脚本对源码目录的写入 |
| 独立源码副本 | 临时试验不覆盖原 Makefile、产物，容易隔离旧打包流程 | 副本会过期；修改原工程不会同步，需要重新核对源码和二进制 |

“套件”和“构建目录”不同：套件选编译器、Qt、GDB、设备；Debug 配置选参数；构建目录放生成文件。通常可以继续使用原 ARM 套件，无需复制工程，也无需每次换一个带日期的目录。

以前创建的验证副本只是当时的隔离措施，不是必需路径。原套件内完整的独立 Debug 构建/部署尚未在前次验证中走通，不应直接声称已经配置好。

本类旧工程要特别检查：

- .pro 中 DESTDIR、OBJECTS_DIR、UI_DIR、MOC_DIR 的实际取值。
- QMAKE_PRE_LINK、QMAKE_POST_LINK，以及 qmake 的 system() 调用。
- 翻译生成、UI 重命名、加密、升级包、U 盘拷贝是否仍写入源码目录。

如果暂时不能隔离这些副作用，先使用副本验证，再整理成可复用的 Debug 配置。不进行无关业务重构。

## 5. 准备可调试的 ARM 程序

先检查已有产物，不按 bin_debug/bin_release 的名称推断架构和符号：

```sh
file <本地ELF>
readelf -S <本地ELF> | grep -E 'debug_info|debug_line'
```

需要 ARM ELF，并有源码调试信息。not stripped 不等于存在源码行号；曾发现名为 Debug 的产物实际是 x86-64 桌面程序，不能上传给手控。

若需构建，加载当前 Kit 使用的 environment-setup，用对应 qmake 和 make 构建。源码调试通常使用 -g；初次验证采用 -O0，便于逐行观察。新目录首次完整构建；旧产物改变参数后确保相关目标重新编译，不能沿用旧对象。

在隔离验证副本中，以下 qmake 参数组合曾成功生成调试信息：

```sh
. <当前ARM编译链的environment-setup>
cd <已准备好的验证副本>
qmake2 <工程.pro> -after \
  'CONFIG-=release' 'CONFIG+=debug' \
  'QMAKE_CFLAGS_DEBUG=-g -O0' 'QMAKE_CXXFLAGS_DEBUG=-g -O0' \
  'QMAKE_PRE_LINK=' 'QMAKE_POST_LINK='
make -j4
```

这不是可对所有工程照抄的发布命令。清空前后处理仅用于已核对的调试验证；正式部署可能需要其中的资源生成步骤。-after 覆盖可能晚于输出目录名求值，所以最终仍应检查 Makefile、ELF 和符号，不凭名称判断。

本次工具链的 ARM GDB 是 7.5.1，不支持 -nh，使用 -nx。根据当前 Kit 找 GDB，不直接拿 Ubuntu 桌面 GDB 替代。

在 GDB 中离线检查请求位置：

```gdb
file <本地ELF绝对路径>
info line <源文件>:<行号>
```

如解析失败，先修复符号/源码匹配，不把函数入口断点冒充指定行断点。

## 6. 上传并保留原程序

优先上传为独立调试文件，保留原程序。确需覆盖时先备份并核对备份。使用当前工程需要的资源、工作目录和运行环境。

Ubuntu 侧：

```sh
sftp <手控用户>@<手控地址>
```

在 SFTP 提示符中：

```text
put <本地ELF> <手控调试文件绝对路径>
bye
```

手控侧设置执行权限，并在两端比较上传文件校验值。GDB 本地符号和手控运行文件必须来自同一次构建；分离调试符号时另外核对构建身份。

前次手控 SCP 报 /usr/bin/scp: Permission denied，SFTP 成功，不必修改系统 scp 权限。

## 7. 连接、断点、单步和变量

以下均为模板，不含固定业务文件或行号。

### 附加正在运行的进程

手控端确认 PID、exe 和 TracerPid 后：

```sh
gdbserver --attach :<空闲端口> <当前PID>
```

附加会暂停目标。纯日志任务不执行此步骤。

### 从程序启动时调试

构造函数、初始化位置可能早已执行，应从启动时设置断点。记录原运行方式，停止经过身份核对的旧进程，确认没有另一份程序占用界面后，在手控运行：

```sh
cd <实际工作目录>
gdbserver :<空闲端口> <调试程序路径> <实际启动参数>
```

手控登录环境中的 QWS、字体、触摸、动态库路径需保留。不能假设非登录 SSH 命令自动拥有相同环境。

### Ubuntu GDB 会话

在另一个 Ubuntu 终端启动匹配的 ARM GDB，在 GDB 提示符执行：

```gdb
set pagination off
set remotetimeout 5
set sysroot <匹配手控的sysroot>
file <本地ELF绝对路径>
target remote <手控地址>:<端口>
tbreak <用户指定源文件>:<用户指定行号>
continue
```

若调试符号中的源码路径与现路径不同，核对内容后使用 set substitute-path 映射。不能仅改路径掩盖源码版本不一致。

命中后：

```gdb
frame
print <关注变量或成员表达式>
next
frame
print <关注变量或成员表达式>
```

next 是源码单步，stepi 是指令单步。优化代码可能跳行或显示变量被优化，不能把读不到当成变量为零。tbreak 命中后自动删除；要长期保留可使用 break，并记录本次新增断点编号。

continue 会等待真实触发，批处理不能无限无人看管。设置成功、实际命中、单步完成需分别核对结果。没有活着的 GDB 会话不能声称仍在等断点。

结束时：

```gdb
detach
quit
```

失败或超时后先检查目标状态，不盲目继续、重连或 kill 调试器。解除附加后进程通常继续，仍需实际核对。

## 8. 结束与恢复

按用户要求保留暂停或结束会话。结束时清理本次新增断点，保留原有断点。若切换过调试程序，解除调试后停止核对过的调试进程，再按原目录、环境、参数启动原程序。

```sh
readlink /proc/<恢复后PID>/exe
grep -E '^(State|TracerPid):' /proc/<恢复后PID>/status
```

确认原文件身份、初始化日志、TracerPid=0 和无残留调试端口占用。S (sleeping) 是等待状态，不代表调试暂停。

调试暂停可能引起上下位机通信报警，应记录暂停时段，避免把测试引发的报警误判为业务问题。

## 9. 读取日志和最近操作

先核对当前进程工作目录和实际输出文件。下面名称来自已验证工程，切换工程后应查代码确认：

| 日志 | 用途 | 边界 |
| --- | --- | --- |
| Modify.log | 权限、模具和参数操作记录 | 只覆盖实际调用事件记录接口的行为 |
| Alarm.log | 报警发生及解除记录 | 历史报警不代表现在仍报警 |
| 程序 stdout/stderr 文件 | 启动、按键、状态等打印 | 路径由启动时重定向决定，可能无时间戳 |

```sh
date
tail -n 20 <当前工作目录>/Modify.log
tail -n 20 <当前工作目录>/Alarm.log
tail -n 60 <本次进程实际输出文件>
```

已验证的操作日志格式是“时间,事件编号,新值,旧值”；报警记录含报警号、发生时间、解除时间或未解决标志。解释编号前核对当前工程的定义、报警字串及调用点。

三档开关、按键可能没有完整日志，不能据一次状态读数还原切换过程。用户问“现在呢”时比较上次快照；AI 自己产生的权限或程序切换也要与用户动作区分。设备时间曾为 2000 年，读取时核对设备时钟。

没有后台服务时不声称持续监视。读取已有日志不暂停、不改写日志。

## 10. 权限切换与当前能力边界

运行时调用程序的权限切换函数已验证，会更新权限标签及关联通知。它属于主动改变现场，需要用户明确要求；不能混在读变量任务里执行。它不是密码登录验证，也不会自动补齐密码字符串等相关状态，不能等同于全部密码相关权限已验证。

已验证：SSH/SFTP、程序启停、函数和源码断点、指令和源码单步、变量读取、操作及报警日志读取。

尚未完成：通用 Status/Read/AddBreakpoint 脚本封装、Qt Creator 会话同步、全部 Qt 库内部符号和对象美化、跨所有工程的自动构建适配。当前是已验证的命令流程，AI 根据本次工程组织执行。

每次交付说明：实际调试的程序、是否命中、读到什么、是否恢复、剩余限制。命令发送成功不等于设备行为已完成。
