# 项目协作规则

## 联机调试

- 用户要求 IAR 联机调试时，先阅读目标下位机工程根目录下的 `docs/IAR现有会话命令协同调试使用手册.md`，按手册复用已有 IAR 会话。
- 用户要求手控联机、日志或调试时，先阅读目标上位机工程根目录下的 `docs/手控SSH命令协同调试使用手册.md`，使用 SSH/GDB 命令，不操作 Qt Creator 控件。
- 根据当前任务和工作区识别目标工程；手册不存在时先查找实际位置，不沿用其他工程的路径或配置。